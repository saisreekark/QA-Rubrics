# Session / KT note — 2026-06-05

Theme: **trust the ruler before chasing the number.** Audited the gold set,
fixed the measurement window for the taxonomy cutover, re-baselined, calibrated
the matcher on the remaining frameworks, reverted the failed Reopen fix v1, and
started a 3.5-flash vs 3.1-pro head-to-head. Pairs with
`docs/gold-audit-2026-06-05.md` and the tracker in `docs/execution-plan.md`.
Demo gate unchanged: accuracy → 80% by **2026-06-15**. Nothing pushed to prod.

## 1. Gold audit — the gold is clean, and we were mis-windowing it
New read-only tool `evaluator/gold_audit.py` (`docs/gold-audit-2026-06-05.md`):
- QA-2026 driver tab = **2,691 clean usable rows, Jan–Apr 2026** (0 dups, 0 blank
  labels, 0 placeholders, join health 97–100%). April is the largest/latest
  labelled month (no May/Jun yet).
- The evaluator had been hard-locked to **Feb/Mar only**, discarding January and
  *all of April*.

## 2. Taxonomy cutover is **March** — window corrected to March-onward
- **The updated taxonomy is only in use from March 2026** for (at least some)
  frameworks (Sai). Jan/Feb are OLD-taxonomy and must not be scored as new-rubric
  gold. **Audit proof:** Quarter Freeze / YoY appears in **0 cases before March**
  across every framework, then jumps in. (`Product/Tools Gap` shows in Jan/Feb but
  predates the rebuild — not evidence of new taxonomy.)
- **`cfg.QA2026_MONTHS` → `("2026-03","2026-04")`** = **1,778 valid gold cases**
  (TTR 860 / Reopen 654 / Escalation 211 / DSAT 53). Explicit allow-list (fixed
  A/B denominator); add the next month deliberately as QA labels it. Memory
  `project_taxonomy_cutover_march`.

## 3. Re-baseline on March–April — new anchor ~12.9%
Frozen production preds (`rebaseline_marapr_2026-06-05T01-58-43Z.csv`):
**TTR 18.65% (858) / Reopen 6.94% (648) / Escalation 7.46% (201) / DSAT 6.00%
(50); overall ~12.9% (1,415 audited).** Supersedes Feb/Mar 12.99% and old-dump
12.93%. Dropping pre-cutover Jan/Feb *recovered* Escalation/DSAT vs a
(discarded) Jan–Apr run that was depressed by old-taxonomy rows. Reopen is the
floor.

## 4. Matcher calibration — generalised + 3 frameworks validated
- Promoted the Reopen one-off `_calib_reopen.py` → tracked
  **`evaluator/calibrate_matcher.py`** (`--framework`, env-tunable concurrency);
  deleted the one-off.
- **All four matchers PASS** (45 cases each, hand-read defensible): Reopen ~90%
  (prior), TTR ~95%, DSAT sound, **Escalation sound** (re-run clean on the ADC
  auth fix). Every accuracy number is now quotable.
- **HEADLINE systemic finding: Reopen, DSAT AND Escalation all share ONE root
  cause** — the bot over-attributes to the **agent (People Gap) / Process Gap**
  and **under-predicts User Gap** (and Product/Tools Gap). DSAT 0/45, Escalation
  2/45, both dominated by this; Reopen the same (0/28 User Gap). Only TTR is less
  affected. → **fix-v2 (aggregator) should lift all three**, not just Reopen.

## 5. Reverted Reopen fix v1 (kept the driver diff)
Fix v1 (sub-agent intent gate) failed (aggregator overrides it). Reverted the
intent-gate text in `prompts/reopen/agent2.md` (now == baseline) and `agent3.md`
(kept only the confirmed driver diff: Quarter Freeze L3 + Product/Tools Gap),
re-injected into the local notebook (`inject_prompts.py`). Clean control for the
future fix-v2. Nothing pushed.

## 6. Infra: matcher auth hardened + speed bumped + model probe
- **Matcher auth → ADC auto-refresh.** The matcher's Vertex `vertexai.init` now
  uses `regen._build_vertex_credentials()` (auto-refreshing ADC, cloud-platform
  scope) instead of the one-shot `GOOGLE_OAUTH_TOKEN`, which **401s ~1h in and
  silently shrinks the denominator** on long runs (this killed the first
  Escalation calibration and would have corrupted a long A/B). Sheets reads keep
  the drive-scoped static token. Verified: ADC regional generate returns OK.
- **Also fixed `ab_measure --months`** default (was hard-coded Feb/Mar) →
  `cfg.QA2026_MONTHS`.
- **Speed.** Bot regen is the bottleneck (full multi-agent pipeline per case);
  it runs on the **global** endpoint (separate quota pool), so concurrency can go
  well above the regional default. Bumped: calibrate `REGEN_CONCURRENCY` 2→12,
  matcher 12→24; `ab_measure` regen 12 (global) / matcher burst 16; both
  env-tunable. Result: ~16 min/framework → ~**5 min/framework**.
- **Model availability re-probed** (`/tmp/probe_models.py`): only **two** Gemini
  3.x callable — `gemini-3.1-pro-preview` (newest pro) and `gemini-3.5-flash`.
  **There is NO 3.5-pro** (404 every spelling); also dead: `3.5-flash-preview`,
  `3-pro-preview`. Memory `reference_models_and_measurement`.

## 7. 3.5-flash vs 3.1-pro A/B (March–April, regen/no-cache, N=3×40, TTR+Reopen)
Regen/no-cache absolutes run below frozen-prod (KB docs unreadable locally) — the
**model-to-model delta** is the signal.
- **3.1-pro** (`ab_PRO31_MARAPR_…json`): **TTR 16.1% ± 8.0** (16.1/19.4/12.9),
  **Reopen 13.3% ± 16.6** (20.0/13.3/6.7). Reopen ≈2× the 2.5 frozen baseline.
- **3.5-flash** (`ab_FLASH35_MARAPR_…json`): **TTR 9.7% ± 13.9** (12.9/12.9/3.2),
  **Reopen 11.1% ± 9.6** (13.3/6.7/13.3).
- **Read: flash is NOT better than pro.** 3.1-pro is directionally ahead on both
  (clearly on TTR 16.1 vs 9.7, marginally on Reopen), but N=3×40 CIs overlap so
  it's not statistically separated. **Stay on 3.1-pro for accuracy;** flash only
  worth revisiting for cost/throughput, and only after N=5×120 confirms it's close.
- ⚠ A background sandbox restart killed the original chained job after the
  3.1-pro run; re-ran flash standalone. Run measurement jobs so the JSON lands in
  `evaluator/runs/` (survives restarts); avoid long chained background jobs.

## 8. Where we are / next
1. **Finish the flash-vs-pro read** + the Escalation calibration re-run.
2. **Take the model lever to Zaidul/Pooja** — 3.1-pro (and possibly 3.5-flash if
   it holds up) vs prompt tuning ≈0. The demo decision.
3. **Fix v2 at the aggregator** now covers **both Reopen AND DSAT** (shared
   agent-over-blame bias) — user-side causes win unless clear independent agent
   failure. Measure on March–April, hold the endpoint constant.
4. **Calibrate Escalation** (re-run landing), then all four are validated.
5. Ask Zaidul: blank `quality_reviewer` (~40% of TTR/Reopen rows), multi-L1 gold
   labels, KSI + Quality consolidation.

## Code touched this session (all local, nothing pushed)
`evaluator/gold_audit.py` (new), `evaluator/calibrate_matcher.py` (new; replaces
`_calib_reopen.py`, deleted), `evaluator/config.py` (QA2026_MONTHS → March–April),
`evaluator/regen.py` (`_build_vertex_credentials` ADC), `evaluator/ab_measure.py`
(ADC matcher init, `--months` default, speed knobs), `prompts/reopen/{agent2,
agent3}.md` (fix v1 reverted, driver diff kept), `notebook/content.ipynb`
(re-injected). Plus docs (`gold-audit-2026-06-05.md`, this note, `execution-plan`,
`CLAUDE.md`) and memories (`project_taxonomy_cutover_march` new, `project_gold_qa2026`,
`reference_models_and_measurement`).
